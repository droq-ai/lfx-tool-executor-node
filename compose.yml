services:
  tool-executor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lfx-tool-executor-node
    environment:
      - HOST=0.0.0.0
      - PORT=8005
      - LOG_LEVEL=INFO
    ports:
      - "8005:8005"
    restart: unless-stopped

